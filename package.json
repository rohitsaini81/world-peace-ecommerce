{
  "name": "world_peace",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install": "npm run install --prefix backend && npm run install --prefix frontend",
    "test" : "npm run dev --prefix frontEnd"
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
